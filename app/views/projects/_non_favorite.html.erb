<%= link_to favorite_path(project.id), id: "fav-#{project.id}", remote: true, method: :delete do %>
  <p>
    <div class="field center">
      <h1 class="fav-true">❤</h1>
    </div>
  </p>
<% end %>